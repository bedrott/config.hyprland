gsettings set org.gnome.desktop.interface color-scheme "prefer-dark"

install this program:

sudo pacman -S nwg-look

and execute him:

nwg-look

and set "prefer-dark".

done! now your setup is set in the Dark Mode :)
